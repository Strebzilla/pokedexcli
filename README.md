# pokedexcli

This is a guided project from [Boot.dev](https://www.boot.dev). This project uses Go to create a repl cli application that can get data from [Poke API](https://pokeapi.co/).

## Setup

```bash
# Runs tests
go test ./...

# Build the project
go build
```


## Usage
```bash
./pokedexcli
```